import { AppBar, Toolbar } from "@material-ui/core";
function NavBar() {
  return (
    <AppBar position="static">
      <Toolbar>Screening Project</Toolbar>
    </AppBar>
  );
}

export default NavBar;
